import React from 'react';
import { IProduct } from '../../models/product';

interface Props {
  product: IProduct;
}

const SingleProductComponent: React.FC<Props> = ({ product }) => {
  return <></>;
};

export default SingleProductComponent;
